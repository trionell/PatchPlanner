/**
 * Display titles for every editable planning vocabulary, shared by the
 * event Settings tab and the "My Defaults" page (Slice 17). Keep in sync
 * with backend domain.Vocabularies.
 */
export const vocabularyTitles: Record<string, string> = {
  signal_types: 'Signal types',
  preamp_connectors: 'Preamp connectors',
  signal_cable_types: 'Signal cable types',
  speaker_cable_types: 'Speaker cable types',
  output_types: 'Output types',
  mic_stands: 'Mic stands',
  power_connectors: 'Power connectors',
  truss_types: 'Truss types',
  channel_colors: 'Channel colors',
}
